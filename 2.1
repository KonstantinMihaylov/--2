#include <stdio.h>

long get_fibbonachi_via_cycle(long int number)
{
  long int element_1 = 1;
  long int element_2 = 1;
  long int next_number;

  if (number <= 2)
  {
    return 1;
  }
  else 
  {
    for(int i = 3; i <= number; i++) 
    {
      next_number = element_1 + element_2; 
      element_1 = element2; 
      element_2 = next_number; 
    }
  }

  return F2;
}
